{
  "name": "trilat",
  "version": "1.0.0",
  "description": "simple computation of trilateration using matlab's Levenberg Marquardt curve-fitting algorithm.'",
  "main": "index.js",
  "scripts": {
    "dist": "browserify -e distgen.js -o dist.js"
  },
  "license": "ISC",
  "dependencies": {
    "ml-curve-fitting": "^0.0.7"
  },
  "devDependencies": {
    "browserify": "^13.1.0"
  }
}
